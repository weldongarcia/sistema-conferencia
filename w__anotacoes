cd backend
uvicorn app.main:app --reload

python -m uvicorn app.main:app --reload
 #rodar app

http://127.0.0.1:8000/docs


✅ 1️⃣ Abrir o PostgreSQL (psql)

No terminal (fora do uvicorn):c

psql -U postgres

👉 Digite a senha que você definiu

✅ 2️⃣ Conectar no banco correto
\c conferencia

✔️ Confirmação esperada:

You are now connected to database "conferencia"

REGRA DE OURO AGORA

Sempre que entrar no PostgreSQL:

\c conferencia

Quando fizer mudanças assim (grandes):
git add .
git commit -m "descrição clara da feature"
git push


Limpar banco de dados
🔥 LIMPE TUDO MESMO
DELETE FROM itens_nf;
DELETE FROM notas_fiscais;
DELETE FROM contagens;